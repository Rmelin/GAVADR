export function WorkBadge({ children, tone = "aqua" }: { children: React.ReactNode; tone?: "aqua" | "red" | "amber" | "blue" }) { return <span className={`work-badge work-badge--${tone}`}>{children}</span>; }
export function WorkState({ loading, error, noun, retry }: { loading: boolean; error: boolean; noun: string; retry: () => void }) { if (loading) return <div className="incident-state" role="status"><span className="loader" />Indlæser {noun}…</div>; if (error) return <div className="incident-state incident-state--error" role="alert"><strong>{noun[0].toUpperCase() + noun.slice(1)} kunne ikke hentes</strong><button type="button" onClick={retry}>Prøv igen</button></div>; return null; }
export function formatWorkDate(value?: string | null) { if (!value) return "Ikke angivet"; const dateOnly = /^\d{4}-\d{2}-\d{2}$/.test(value); return new Intl.DateTimeFormat("da-DK", dateOnly ? { dateStyle: "medium", timeZone: "UTC" } : { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)); }
